//User Interaction: Prompt dialog box

let boxWidth=window.prompt("Enter width:");
    
let boxHeight=window.prompt("Enter height:");
    
let boxLength=window.prompt("Enter length:");
    
let boxVolume= boxWidth*boxHeight*boxLength;

window.alert ("The volume of the box is " + boxVolume+".")
